version: "3.9"

services:
  backend:
    build: ./bk
    container_name: backend

    ports:
      - "5000:5000"

    env_file:
      - ./bk/.env

    volumes:
      - ./bk:/app
      - /app/node_modules

    restart: unless-stopped